"use client";

import { SocialAccountsManager } from './social-accounts-manager';

// This is a wrapper component to match the expected import name
export function SocialAccountsForm() {
  return <SocialAccountsManager />;
}
